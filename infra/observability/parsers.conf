# Fluent Bit parsers — structured logging

[PARSER]
    Name        json
    Format      json
    Time_Key    time
    Time_Format %Y-%m-%dT%H:%M:%S.%LZ

[PARSER]
    Name        nexus_log
    Format      regex
    Regex       ^(?<time>[^ ]+) (?<level>\w+) \[(?<trace_id>[^\]]*)\] \[(?<correlation_id>[^\]]*)\] (?<message>.*)$
    Time_Key    time
    Time_Format %Y-%m-%dT%H:%M:%S.%L
