# Infra

Infrastructure and deployment configs for Nexus Engine.

- **docker/** — Dockerfiles and compose (see repo root `docker-compose.yml`)
- **k8s/** — Kubernetes manifests (add as needed)
- **scripts/** — Deploy and utility scripts (add as needed)
