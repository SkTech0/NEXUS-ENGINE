# Engine-intelligence service shell — independent K8s deployment.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: engine-intelligence
  labels:
    app: engine-intelligence
    tier: service-shell
spec:
  replicas: 1
  selector:
    matchLabels:
      app: engine-intelligence
  template:
    metadata:
      labels:
        app: engine-intelligence
        tier: service-shell
    spec:
      containers:
        - name: engine-intelligence
          image: nexus-engine-intelligence:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3004
              name: http
          env:
            - name: ENGINE_INTELLIGENCE_SERVICE_PORT
              value: "3004"
            - name: NEXUS_ENV
              valueFrom:
                configMapKeyRef:
                  name: nexus-env
                  key: NEXUS_ENV
                  optional: true
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /ready
              port: 3004
            initialDelaySeconds: 3
            periodSeconds: 5
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: engine-intelligence
  labels:
    app: engine-intelligence
spec:
  type: ClusterIP
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
      name: http
  selector:
    app: engine-intelligence
