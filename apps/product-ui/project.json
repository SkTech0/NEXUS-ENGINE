{
  "name": "product-ui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/product-ui/src",
  "tags": ["scope:ui", "type:app"],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": ".",
        "command": "npx ng serve product-ui --configuration development --proxy-config apps/product-ui/proxy.conf.json"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/product-ui",
        "command": "npx ng build product-ui --configuration development"
      }
    }
  }
}
