{
  "name": "engine-validation",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "engine-validation/src",
  "tags": ["scope:validation", "domain:engine"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/engine-validation",
        "main": "engine-validation/src/index.ts",
        "tsConfig": "engine-validation/tsconfig.json"
      },
      "configurations": {
        "production": {}
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "engine-validation/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": { "ci": true }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["engine-validation/**/*.ts"]
      }
    }
  }
}
