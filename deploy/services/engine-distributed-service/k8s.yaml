apiVersion: apps/v1
kind: Deployment
metadata:
  name: engine-distributed-service
  labels:
    app: engine-distributed-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: engine-distributed-service
  template:
    metadata:
      labels:
        app: engine-distributed-service
    spec:
      containers:
        - name: engine-distributed-service
          image: nexus/engine-distributed-service:latest
          ports:
            - containerPort: 5016
          livenessProbe:
            httpGet:
              path: /health
              port: 5016
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 5016
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: engine-distributed-service
spec:
  selector:
    app: engine-distributed-service
  ports:
    - port: 5016
      targetPort: 5016
  type: ClusterIP
