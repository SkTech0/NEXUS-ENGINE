apiVersion: apps/v1
kind: Deployment
metadata:
  name: engine-intelligence-service
  labels:
    app: engine-intelligence-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: engine-intelligence-service
  template:
    metadata:
      labels:
        app: engine-intelligence-service
    spec:
      containers:
        - name: engine-intelligence-service
          image: nexus/engine-intelligence-service:latest
          ports:
            - containerPort: 5012
          livenessProbe:
            httpGet:
              path: /health
              port: 5012
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 5012
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: engine-intelligence-service
spec:
  selector:
    app: engine-intelligence-service
  ports:
    - port: 5012
      targetPort: 5012
  type: ClusterIP
