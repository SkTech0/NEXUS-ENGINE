apiVersion: apps/v1
kind: Deployment
metadata:
  name: engine-data-service
  labels:
    app: engine-data-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: engine-data-service
  template:
    metadata:
      labels:
        app: engine-data-service
    spec:
      containers:
        - name: engine-data-service
          image: nexus/engine-data-service:latest
          ports:
            - containerPort: 5015
          livenessProbe:
            httpGet:
              path: /health
              port: 5015
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 5015
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: engine-data-service
spec:
  selector:
    app: engine-data-service
  ports:
    - port: 5015
      targetPort: 5015
  type: ClusterIP
