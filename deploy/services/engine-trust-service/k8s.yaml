apiVersion: apps/v1
kind: Deployment
metadata:
  name: engine-trust-service
  labels:
    app: engine-trust-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: engine-trust-service
  template:
    metadata:
      labels:
        app: engine-trust-service
    spec:
      containers:
        - name: engine-trust-service
          image: nexus/engine-trust-service:latest
          ports:
            - containerPort: 5014
          livenessProbe:
            httpGet:
              path: /health
              port: 5014
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 5014
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: engine-trust-service
spec:
  selector:
    app: engine-trust-service
  ports:
    - port: 5014
      targetPort: 5014
  type: ClusterIP
